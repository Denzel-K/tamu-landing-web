# tamu-nexus-gateway environment example
# Copy this file to `.env.local` and adjust values as needed.

# API base URL configuration (preferred)
# These are read by the app runtime (not Vite-specific) via window.NEXT_* when available.
# In local dev, you can also export them in your shell before `npm run dev` and inject
# via an inline script tag, or rely on the fallback Vite variables below.
NEXT_WEB_API_BASE_DEV=http://localhost:3000

# Production base URL(s) for TAMU-WEB-APP (Next.js) API
# Accepts a single URL or a comma-separated list of URLs.
# When multiple are provided, the app will use the FIRST non-empty entry.
# Example (single):
# NEXT_WEB_API_BASE=https://tamu-business.onrender.com
# Example (multiple):
# NEXT_WEB_API_BASE=https://tamu-business.onrender.com,https://tamu-business.onrender.com
NEXT_WEB_API_BASE=https://tamu-business.onrender.com

# Optional Vite fallbacks (used only if NEXT_* are not set at runtime)
# These are read from import.meta.env by Vite during dev/build.
VITE_WEB_APP_BASE=https://tamu-business.onrender.com
VITE_SOCKET_URL=

# Optional socket base URL alternative (runtime)
# If set on window.NEXT_SOCKET_URL, it will take precedence over VITE_SOCKET_URL.
# Example:
# NEXT_SOCKET_URL=wss://your-socket-host.example.com
NEXT_SOCKET_URL=

# Mobile Backend API (TAMU-MOBILE-BACKEND)
# The app selects which URL to use based on MODE (development/production):
# - In development: prefers VITE_MOBILE_API_DEV_BASE_URL, falls back to NGROK, then localhost:5000
# - In production: prefers VITE_MOBILE_API_PROD_BASE_URL, falls back to NGROK
# You can always force a specific URL with VITE_MOBILE_API_BASE_URL

# Explicit override (highest priority in all modes) — include /api or not (auto-appended)
VITE_MOBILE_API_BASE_URL=

# Dev base (preferred in MODE=development)
VITE_MOBILE_API_DEV_BASE_URL=http://localhost:5000

# Prod base (preferred in MODE=production)
VITE_MOBILE_API_PROD_BASE_URL=https://your-production-api.example.com

# Optional: ngrok tunnel
VITE_MOBILE_API_NGROK_URL=

# Client-side guard for the private Manage Testers page.
# This should match the ADMIN_PASS configured in TAMU-MOBILE-BACKEND.
VITE_ADMIN_PASS=
